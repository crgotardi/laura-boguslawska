---
import type { InputType } from '@typings/Input';
import Label from './Label.astro'
import HelperText from './HelperText.astro'

interface Props {
    id: string,
    name: string,
    placeholder: string,
    type: InputType,
    label: string,
    ariaLabel: string,
    error: boolean,
    helperText: string;
}

const { 
    id, 
    name, 
    label, 
    ariaLabel, 
    placeholder, 
    type, 
    error,
    helperText
} = Astro.props;
---

<div 
    class="form-section flex flex-col"
    aria-label="Form section"
>
    <Label id={id}>
        {label}
    </Label>
    <input
        id={id}
        class:list={[
            'text-sm',
            'text-gray',
            'form-input',
            'rounded',
            'bg-paper-200',
            'px-4',
            'py-2',
            'outline-none',
            { 'border-solid border-2 border-danger': error }
        ]}
        name={name}
        type={type}
        aria-label={ariaLabel}
        placeholder={placeholder}
    />
    <HelperText error={error}>
        {helperText}
    </HelperText>
</div>